#include<stdio.h>
#include<string>

int main()
{
 char menu[10]="*1212*1#";
 std::string service;
service.assign(menu);
 printf("service:%s[%d]",service.substr(0,service.find('#')).c_str(),service.length());
 return 0;
}
